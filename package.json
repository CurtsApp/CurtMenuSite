{
  "name": "curtmenusite",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "deploy": "next build && next export && type nul >> \"out/.nojekyll\" && git add out/ && git commit -m \"Deploy Next.js to remote\" && git subtree push --prefix out origin gh-pages"
  },
  "dependencies": {
    "next": "10.2.3",
    "react": "17.0.2",
    "react-dom": "17.0.2"
  }
}